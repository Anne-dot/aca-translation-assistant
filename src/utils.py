"""
TODO:
	ensure_output_dir (as `ensure_directory_exists`),
	read_csv_file (as `read_csv_rows`),
	(load|save)_json_file,
in tools/filemanage

	normalize_term, clean_text_for_csv, shorten_text, clean_text,
	parse_list_from_text, detect_numbered_meanings, split_numbered_text,
in tools/text_processing

	collect_normalization_issues,
in tools/normalization_detection
"""